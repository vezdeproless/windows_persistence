[project]
name = "windows-persistence-detector"
version = "0.1.0"
description = "Windows persistence artifact acquisition and Wazuh normalization helpers"
requires-python = ">=3.10"
readme = "README.md"

[project.scripts]
persist-detector = "persist_detector.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
